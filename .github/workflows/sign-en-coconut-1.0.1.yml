name: Sign EN Coconut Chunks 1.0.1

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/sign-en-coconut-1.0.1.yml"
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  sign-validated-apk:
    name: Sign validated EN Coconut Chunks 1.0.1 APK
    runs-on: ubuntu-latest
    steps:
      - name: Download validated unsigned APK
        uses: actions/download-artifact@v4
        with:
          repository: vielleichtistdasso-arch/CoconutChunks
          run-id: 31246287690
          name: EN-Coconut-Chunks-1.0.1-unsigned-release
          path: ${{ runner.temp }}/unsigned
          github-token: ${{ github.token }}

      - name: Verify validated APK identity
        shell: bash
        run: |
          set -euo pipefail
          aapt="$ANDROID_HOME/build-tools/36.0.0/aapt"
          test -x "$aapt"
          unsigned_apk="$(find "$RUNNER_TEMP/unsigned" -type f -name '*.apk' -print -quit)"
          test -n "$unsigned_apk"
          cp "$unsigned_apk" "$RUNNER_TEMP/EN-Coconut-Chunks-1.0.1-unsigned.apk"

          badging="$("$aapt" dump badging "$RUNNER_TEMP/EN-Coconut-Chunks-1.0.1-unsigned.apk")"
          printf '%s\n' "$badging" | grep -F "package: name='com.coconutchunks.app' versionCode='2' versionName='1.0.1'"
          printf '%s\n' "$badging" | grep -F "application-label:'EN Coconut Chunks'"

      - name: Restore release signing key
        shell: bash
        env:
          KEYSTORE_BASE64: ${{ secrets.COCONUT_KEYSTORE_BASE64 }}
        run: |
          set -euo pipefail
          normalized_keystore_base64="$(printf '%s' "$KEYSTORE_BASE64" | tr -d '[:space:]')"
          test -n "$normalized_keystore_base64"
          printf '%s' "$normalized_keystore_base64" | base64 --decode > "$RUNNER_TEMP/coconut-chunks-release.jks"
          test -s "$RUNNER_TEMP/coconut-chunks-release.jks"

      - name: Sign and verify release APK
        shell: bash
        env:
          KEYSTORE_PASSWORD: ${{ secrets.COCONUT_KEYSTORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.COCONUT_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          apksigner="$ANDROID_HOME/build-tools/36.0.0/apksigner"
          unsigned_apk="$RUNNER_TEMP/EN-Coconut-Chunks-1.0.1-unsigned.apk"
          signed_apk="$RUNNER_TEMP/EN-Coconut-Chunks-1.0.1-release.apk"
          keystore="$RUNNER_TEMP/coconut-chunks-release.jks"

          test -x "$apksigner"
          test -s "$unsigned_apk"
          test -s "$keystore"
          test -n "$KEYSTORE_PASSWORD"
          test -n "$KEY_PASSWORD"

          "$apksigner" sign \
            --ks "$keystore" \
            --ks-key-alias "coconut-chunks-release" \
            --ks-pass env:KEYSTORE_PASSWORD \
            --key-pass env:KEY_PASSWORD \
            --out "$signed_apk" \
            "$unsigned_apk"

          verify_output="$("$apksigner" verify --verbose --print-certs "$signed_apk")"
          printf '%s\n' "$verify_output"

          actual_fingerprint="$(printf '%s\n' "$verify_output" | sed -n 's/^Signer #1 certificate SHA-256 digest: //p' | head -n 1 | tr '[:upper:]' '[:lower:]' | tr -d ':')"
          expected_fingerprint="cbd52863ae9f22a1f1cb6dc846220d66462dff0c0a8ddb6d76bd416ebd37997c"

          if [[ "$actual_fingerprint" != "$expected_fingerprint" ]]; then
            echo "Release certificate fingerprint mismatch." >&2
            exit 1
          fi

          sha256sum "$signed_apk"

      - name: Remove temporary signing key
        if: always()
        shell: bash
        run: rm -f "$RUNNER_TEMP/coconut-chunks-release.jks"

      - name: Upload signed release APK
        uses: actions/upload-artifact@v4
        with:
          name: EN-Coconut-Chunks-1.0.1-release
          path: ${{ runner.temp }}/EN-Coconut-Chunks-1.0.1-release.apk
          if-no-files-found: error
          retention-days: 90
